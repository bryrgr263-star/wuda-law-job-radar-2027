# Legacy Boundary

This directory is reserved for documentation or deliberately preserved legacy material that is not part of the active ingestion runtime.

The full pre-Phase 0 experimental archive is stored outside the repository. Production files remain in their original paths until a separately approved compatibility migration.
