# Experiments

Experiments are source-specific research and are excluded from the production TypeScript project.

They may inspect archived samples, but they must not be imported by `app/`, `lib/`, or production scripts. Promotion into the mainline requires an Adapter contract, fixtures, and tests.
